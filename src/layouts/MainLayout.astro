---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Para Majito - El Storyboard ❤️',
  description = 'Una historia de amor contada al estilo cómic, con carreras, desafíos y mucho cariño'
} = Astro.props;
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Michroma&family=Roboto+Mono:wght@400;700&family=Syncopate:wght@400;700&display=swap"
    rel="stylesheet"
  />
  
  <!-- SEO & Social -->
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content="website" />
  <meta name="twitter:card" content="summary_large_image" />
  
  <title>{title}</title>
</head>
<body>
  <div id="page-wrapper">
    <slot />
    
    <!-- Footer -->
    <footer id="footer" style="background: var(--color-black); color: var(--color-white); text-align: center; padding: 2rem;">
      <p>&copy; Esteban Jiménez (El Player 2 Eterno)</p>
      <p style="color: var(--color-pink);">Con cariño para Majito - La Player 1 Invencible ❤️🕷️</p>
    </footer>
  </div>
  
  <slot name="scripts" />
</body>
</html>

<style>
  body {
    margin: 0;
    padding: 0;
  }
  
  #page-wrapper {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }
  
  #footer {
    margin-top: auto;
  }
</style>
