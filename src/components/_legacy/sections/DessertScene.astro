---
// Escena 4: Postres/Dessert scene
---

<section id="escena4" class="scene-section">
  <div class="container">
    <div class="comic-panel comic-panel--pink">
      <h2>🍰 ESCENA 4: CRISIS DE LOS 20</h2>
      <p>
        Bajamos las revoluciones y charlamos como <strong>señores de 30 años estresados</strong>,<br>
        cuando apenas vamos a entrar a los 20 fuertes. 😅
      </p>
      <p>
        <strong class="text-cyan">Nos faltó tiempo,</strong> pero conectar así, siendo los dos lojanos perdidos acá, estuvo genial.
      </p>
      <p class="note-box">
        <em>
          (Nota: Sigo con la espina de no haberte acompañado bien a la estación.<br>
          Me sentí un poco "poco hombre" ahí, fallo de seguridad mío.<br>
          <strong>Prometo compensarlo.</strong> 🚉)
        </em>
      </p>
    </div>
  </div>
</section>

<style>
  .scene-section {
    padding: var(--space-xxl) var(--space-lg);
    background: var(--color-white);
  }
  
  .scene-section:nth-child(even) {
    background: linear-gradient(135deg, #FFF5F5 0%, #F5FFFF 100%);
  }
  
  h2 {
    margin-bottom: var(--space-lg);
    text-align: center;
  }
  
  p {
    font-size: 1.1rem;
    line-height: 1.8;
  }
  
  .note-box {
    background: var(--color-white);
    border: 3px dashed var(--color-gray);
    padding: var(--space-md);
    margin-top: var(--space-lg);
    font-size: 0.95rem;
    color: var(--color-gray);
  }
  
  .note-box strong {
    color: var(--color-black);
  }
  
  @media (max-width: 768px) {
    .scene-section {
      padding: var(--space-xl) var(--space-md);
    }
  }
</style>
