---
// Escena 2: Arcade
---

<section id="escena2" class="scene-section">
  <div class="container">
    <div class="comic-panel comic-panel--pink">
      <h2>🎮 ESCENA 2: LA HUMILLACIÓN HISTÓRICA</h2>
      <p>
        Aquí sí, <strong class="text-pink">plot twist total.</strong><br>
        Según yo, tú no eras tan "pro" en los carros... ¡y tómala!
      </p>
      <p>
        <strong>Literal, me humillaste.</strong><br>
        Dos carreras, dos derrotas por milímetros. 🏎️💨
      </p>
      <p>
        Esa sensación de competitividad y de entrar en confianza tan rápido fue lo más cool.<br>
        <strong class="text-cyan">Me encanta que no te dejas ganar ni a balas.</strong> 😂
      </p>
      
      <!-- Scoreboard -->
      <div class="scoreboard">
        <h3 style="text-align: center; font-size: 1.3rem; margin-bottom: 1rem;">
          🏆 MARCADOR FINAL
        </h3>
        <div class="score-display">
          <div class="score-item winner">
            <span class="score-name">MAJITO</span>
            <span class="score-number">2</span>
          </div>
          <div class="score-divider">-</div>
          <div class="score-item loser">
            <span class="score-name">ESTEBAN</span>
            <span class="score-number">0</span>
          </div>
        </div>
        <p class="score-note">
          <small>(Por milímetros, pero cuenta como humillación histórica)</small>
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .scene-section {
    padding: var(--space-xxl) var(--space-lg);
    background: var(--color-white);
  }
  
  .scene-section:nth-child(even) {
    background: linear-gradient(135deg, #FFF5F5 0%, #F5FFFF 100%);
  }
  
  h2 {
    margin-bottom: var(--space-lg);
    text-align: center;
  }
  
  p {
    font-size: 1.1rem;
    line-height: 1.8;
  }
  
  .scoreboard {
    background: var(--color-white);
    border: 4px solid var(--color-black);
    padding: var(--space-lg);
    margin-top: var(--space-xl);
  }
  
  .score-display {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-lg);
    font-family: var(--font-display);
    font-size: 2rem;
  }
  
  .score-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
  }
  
  .score-name {
    font-size: 1rem;
    letter-spacing: 0.1em;
  }
  
  .score-number {
    font-size: 3rem;
    font-weight: 900;
  }
  
  .winner .score-number {
    color: var(--color-pink);
    animation: pulse 2s infinite;
  }
  
  .loser .score-number {
    color: var(--color-gray);
  }
  
  .score-divider {
    font-size: 2rem;
    color: var(--color-black);
  }
   
  .score-note {
    text-align: center;
    margin-top: var(--space-md);
    color: var(--color-gray);
  }
  
  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }
  
  @media (max-width: 768px) {
    .scene-section {
      padding: var(--space-xl) var(--space-md);
    }
    
    .score-display {
      font-size: 1.5rem;
      gap: var(--space-md);
    }
    
    .score-number {
      font-size: 2rem !important;
    }
  }
</style>
